<h1>Enter an address to get its Geo-location</h1>
<%= form_with url: geo_search_path, method: :get, local: true do |f| %>
  <%= f.text_field :address, required: true %>
  <%= f.submit :Search %>
<% end %>
